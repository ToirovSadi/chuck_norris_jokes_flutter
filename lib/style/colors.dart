import 'package:flutter/material.dart';

const lightGreen = Color(0xFFC4FFA9);

const lightGreenSearch = Color.fromARGB(255, 220, 255, 203);

// background color
const customGreen = Color(0xFF65FF52);

// color for text style
const customBlack = Color.fromARGB(255, 0, 0, 0);

// color for text style in categories
const customIndigo = Color.fromARGB(255, 63, 81, 181);
